{
  "name": "unsecuredapikeys-monorepo",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "UnsecuredAPIKeys.UI"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}